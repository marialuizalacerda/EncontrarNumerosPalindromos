import React from 'react';
import ReactDOM from 'react-dom';
import PalindromeApp from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PalindromeApp />
  </React.StrictMode>
)